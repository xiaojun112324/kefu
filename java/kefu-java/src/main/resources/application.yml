# application.yml
server:
  port: 8866

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://103.42.31.144:3306/kefu_simple?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Singapore&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull
    username: kefu_simple
    password: KFfGm3hNeMrifiAy
    hikari:
      pool-name: HikariPool
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 600000         # 10 min
      connection-timeout: 30000    # 30s
      max-lifetime: 1800000        # 30 min(略小于 MySQL wait_timeout)
      auto-commit: true
      validation-timeout: 5000
      connection-test-query: "SELECT 1"

  jackson:
    time-zone: Asia/Singapore
    date-format: yyyy-MM-dd HH:mm:ss

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml     # 你的 Mapper XML 放这里
  type-aliases-package: com.yourapp.**.entity      # 实体包
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl   # 开发期打印 SQL（生产请关闭）
  global-config:
    db-config:
      id-type: AUTO
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 按需开启 Flyway（可选）
# flyway:
#   enabled: true
#   locations: classpath:db/migration
#   baseline-on-migrate: true

logging:
  level:
    root: info
    com.yourapp: debug
